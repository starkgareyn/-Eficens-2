#!/bin/bash

# Setup New Hostname
hostnamectl set-hostname "app-node1.eficens.com"

# Configure New Hostname as part of /etc/hosts file 
echo "`hostname -I | awk '{ print $1}'` `hostname`" >> /etc/hosts

# Update the Repository
sudo apt update